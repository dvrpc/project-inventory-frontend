export const customStyles = {
  control: (base: any) => ({
    ...base,
    borderRadius: '19px',
    borderColor: '#cbd5da',
    '&:hover': {
      borderColor: '#cbd5da',
    },
  }),
  input: (base: any) => ({
    ...base,
    marginLeft: '8px',
  }),
  placeholder: (base: any) => ({
    ...base,
    marginLeft: '8px',
  }),
}
